// Criar um objeto simples
// Crie um objeto chamado pessoa que contenha as propriedades: nome, idade e cidade. Em seguida, acesse o valor de nome e cidade.
let pessoa = {
    nome: 'Railton',
    idade: 34,
    cidade: 'Campinas'
};

console.log(`Nome: ${pessoa.nome} e cidade: ${pessoa.cidade}`);