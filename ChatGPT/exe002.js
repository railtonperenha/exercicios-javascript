// Número par ou ímpar
// Verifique se um número armazenado na variável numero é par ou ímpar. Retorne "Par" ou "Ímpar".

let numero = 10;
if (numero % 2 == 0) {
    console.log('Número Par');
} else {
    console.log('Número Ímpar');
};