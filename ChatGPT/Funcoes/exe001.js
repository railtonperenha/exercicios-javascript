/*
1. Soma de Dois Números
Crie uma função chamada soma que recebe dois números como parâmetros e retorna a soma deles.
*/
function soma (x, y) {
    let result = x + y;
    console.log(`A soma de ${x} + ${y} é ${result}`);
}

soma(10, 4);