// Determinar categoria etária
// Com base na idade armazenada na variável idade, retorne "Criança" (menor que 12), "Adolescente" (entre 12 e 17), ou "Adulto" (18 ou mais).

let idade = 15;
if (idade < 12) {;
    console.log('Criança');
} else if (idade >= 12 && idade < 17) {
    console.log('Adolescente');
} else {
    console.log('Adulto');
};