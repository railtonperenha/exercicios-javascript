// Adicionar propriedades a um objeto
// Crie um objeto chamado carro com as propriedades marca e modelo. Adicione dinamicamente uma nova propriedade chamada ano ao objeto.
const carro = {
    marca: 'Nissan',
    modelo: 'Kicks'
};

// Adicionando a propriedade 'ano' dinamicamente
carro.ano = 2018;
console.log(carro);