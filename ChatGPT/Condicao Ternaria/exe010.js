// Número positivo, negativo ou zero
// Com base no valor da variável numero, retorne "Positivo" se for maior que 0, "Negativo" se for
let numero = 0;
if (numero > 0) {
    console.log('Número Positivo');
} else {
    console.log('Número Negativo');
};